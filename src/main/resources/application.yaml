server:
  address: localhost
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/restaurant_catalogue
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
        ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    # fetch-registry: false
    # register-with-eureka: false
    service-url:
      defaultZone: http://192.168.0.114:8761/eureka/

restaurant:
  restaurant_listing:
    url: http://192.168.0.114:8081
    # url: http://RESTAURANT_LISTING